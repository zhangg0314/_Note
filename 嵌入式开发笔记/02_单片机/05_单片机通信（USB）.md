# ——USB协议——

# 物理特性

## 1.引脚

一条USB传输线分别由地线、电源线、D+和D-四条线构成，D+和D-是差分输入线，它使用的是3.3V的电压(与CMOS的5V电平不同)，而电源线和地线可向设备提供5V电压，最大电流为500mA(可以在编程中设置)。

| 引脚标号 | 信号名称   | 缆线颜色 |
| :------: | ---------- | :------: |
|    1     | Vcc        |    红    |
|    2     | Data- (D-) |    白    |
|    3     | Data+ (D+) |    绿    |
|    4     | GND        |    黑    |

```txt
USB设备可以直接和HOST通信，或者通过Hub和Host通信。一个USB系统中仅有一个USB主机，设备包括USB功能设备和USB HUB(扩展坞)，最多支持127个设备。物理连接指的是USB传输线。在USB 2.0系统中要求使用屏蔽双绞线。
```

## 2.USB 信号（差分信号）

使用差分信号进行数据传输，有效的降低外接带来的干扰。

## 3 .USB 信号编码（NRZI）

USB 数据传输的传输使用反向不归零编码进行传送（NRZI），而UART，IIC等用的是NRZ编码，这样就少一根时钟线也能同步。

反向不归零编码方式可以保证数据的完整性，而且不要求传输过程中由独立的时钟信号，且不会像归零编码那样每传完1bit就归零来占据一位浪费带宽。

## 4.USB 字节序

LSB 先发，MSB 后

## 5.USB 设备检测

USB1.0和USB1.1支持1.5Mb/s的低速模式和12Mb/bs的全速模式。在USB2.0以上支持480Mb/s的高速模式。

当 USB 设备接入系统时刻，系统通过检测 USB 上的 D+或者D- 线上的上拉电阻的方式来识别低速和全速设备。如下图所示，当主机端没有设备接入的时候，其D+和D-的下拉电阻使得其电压几乎为 0V；当全速/低速设备接入后，在D+或者D-端的上拉电阻会使得D+/D-出现高电平，而另外一根是低电平。主机端便知道有设备插入。

![img](D:..\figure\c3226ba58a6555420d06f77dfefb1ab6.png)

对于高速设备，和全速设备一样，在D+上存在上拉电阻，对于高速设备的的识别，主机先把高速设备检测为全速设备，然后再通过“Chirp 序列”的总线握手机制来识别高速和全速设备。

------

# 组帧工作

## 1.介绍

USB的组帧（即数据包的构建与解析）是**硬件与软件协同完成**的过程，具体分工取决于USB控制器的硬件设计和功能复杂度。不同类型的控制器（如简单的低速/全速设备控制器 vs 高速/超高速主机控制器）可能会有不同的实现方式，但核心原则是：**硬件负责物理层和链路层的帧处理，软件负责高层协议的逻辑控制**。

## 2.硬件负责的组帧工作 

USB控制器硬件（通常集成在MCU或专用芯片中）主要处理与物理传输相关的帧构建和解析，包括： 

- **PID（包标识符）生成与校验**：
  硬件自动生成各类数据包的PID域（如OUT、IN、DATA0等），并附加4位校验位；接收时自动校验PID的有效性，过滤错误包。 

-  **同步域（SYNC）和包结束（EOP）信号**：
  硬件负责在数据包开头添加同步序列（用于时钟同步），结尾生成EOP信号（标志包结束），这是物理层的强制要求。 

- **CRC校验码生成与验证**：
  对于数据域（DATA）和令牌域（TOKEN），硬件会自动计算CRC16（数据域）或CRC5（令牌域）校验码并附加到包中；接收时自动验证CRC，丢弃错误包。

- **差分信号编解码**：
  硬件将数字信号转换为USB规范的差分信号（如高速模式下的8b/10b编码），接收时反向解码，软件无需干预。 

- **端点缓冲区管理**：
  硬件直接操作端点的物理缓冲区（如`tx_addr`/`rx_addr`指定的地址），自动完成数据的加载（发送时）或存储（接收时），减少CPU干预。

## 3.软件负责的组帧工作 

软件（通常是USB设备驱动或固件）主要处理高层协议逻辑，控制硬件完成帧的内容填充和传输触发，包括：

-  **配置端点参数**：
  通过设置`usb_ept_info`等结构体（如传输类型、最大包长、方向等），告知硬件端点的通信规则，硬件据此调整组帧方式（如等时传输的包长可动态变化）。 

- **填充数据内容**：
  软件将需要发送的数据（如应用层的命令、文件数据）写入硬件指定的缓冲区（`trans_buf`），硬件会自动将这些数据封装成DATA包。 

- **触发传输动作**：
  软件通过寄存器操作（如设置“发送使能”位）通知硬件开始组帧并发送；接收时，硬件完成帧解析后通过中断通知软件读取数据。 

- **处理特殊帧逻辑**：
  对于控制传输的SETUP包（包含请求类型、命令等），软件需要解析数据域中的具体内容（如`bmRequestType`、`bRequest`字段），并决定后续响应（如返回描述符、设置设备状态）。

-  **错误恢复与状态管理**：
  当硬件检测到帧错误（如CRC失败、PID无效）时，软件负责处理异常（如重试传输、进入STALL状态）。 

## 4.典型流程示例（设备发送数据） 

1. 软件准备好待发送的数据，写入端点的发送缓冲区（`trans_buf`），并设置`total_len`（总长度）、`maxpacket`（最大包长）等参数。 
2. 软件通过寄存器命令触发硬件发送，硬件自动：   
   - 生成对应的PID（如DATA1）； 
   - 从缓冲区读取数据，按`maxpacket`分拆为多个包；  
   - 为每个包添加SYNC域、CRC校验码和EOP信号；  
   - 转换为差分信号发送到USB总线。
3. 主机返回ACK握手包后，硬件通过中断通知软件“传输完成”，软件更新`trans_len`（已传输长度），判断是否需要继续发送剩余数据。 
## 5.总结 

**硬件**：负责物理层和链路层的“机械性”组帧工作（如PID、CRC、SYNC、信号转换等），确保帧格式符合USB电气和协议规范，效率高且实时性强。 

**软件**：负责高层逻辑控制（如数据内容、传输触发、参数配置、错误处理等），决定“传输什么”和“何时传输”，灵活性高。 这种分工既利用了硬件的高速处理能力，又通过软件实现了复杂的协议逻辑，是USB通信高效可靠的核心设计。



------

# USB物理层

## 1.USB PHY

USB PHY（Physical Layer，物理层）是USB接口中负责**物理信号处理**的关键组件，位于 USB 控制器（MAC 层，Media Access Control）与物理传输介质（USB 线缆）之间，是实现 USB 协议物理层规范的硬件模块。其核心作用是将数字信号与物理电信号进行转换，确保数据在 USB 总线上可靠传输。

## 2.USB PHY 主要功能

1. **信号转换（数模 / 模数转换）**
   - **发送端**
     将 USB 控制器输出的数字信号（0/1）转换为符合 USB 协议的物理电信号（如差分电压信号），通过 USB 线缆传输。
   - **接收端**
     将线缆上的物理电信号转换为数字信号，传递给 USB 控制器处理。
2. **符合 USB 电气规范**
   USB 协议对物理层信号有严格定义（如电压、时序、速率），PHY 需严格遵守：
   - **低速（LS，1.5Mbps）** 和**全速（FS，12Mbps）**：使用单端信号（针对 D+、D - 线），通过电平变化表示数据（如 FS 模式下，J 状态为`D+=3.3V, D-=0V`，K 状态相反）。
   - **高速（HS，480Mbps）** 和**超高速（SS，5Gbps/10Gbps）**：使用差分信号（D + 与 D - 的电压差），抗干扰能力更强，支持更高传输速率。
   - 此外，PHY 还需处理信号的阻抗匹配、驱动强度等电气特性，确保信号完整性。
3. **时钟同步与恢复**
   USB 总线上的数据流不含单独的时钟信号，而是通过**曼彻斯特编码**（低速 / 全速）或**8b/10b 编码**（高速 / 超高速）将时钟信息嵌入数据中。PHY 需从接收的信号中提取时钟，实现与发送端的同步，保证数据正确采样。
4. **链路管理**
   - 检测 USB 线缆的插拔（通过 D+、D - 线的上拉 / 下拉电阻状态变化）。
   - 实现设备与主机之间的速率协商（如高速设备插入时，先以全速模式通信，再切换到高速模式）。
   - 处理低功耗模式（如 suspend/resume 信号），控制总线的电源管理。
5. **错误检测**
   对传输的信号进行基本的错误检测（如信号完整性、噪声干扰导致的错误），并将结果反馈给 USB 控制器。

## 3.USB PHY 存在形式

- **独立芯片**
  在早期或复杂的 USB 系统中，PHY 可能是独立的芯片（如 USB PHY 芯片），通过特定接口（如 ULPI、UTMI）与 USB 控制器连接。
- **集成在控制器中**
  现代 SOC（系统级芯片）或 MCU（微控制器）通常将 USB PHY 与 USB 控制器集成在一起（如 STM32、ESP32 等嵌入式芯片），简化硬件设计。

## 4.总结

USB PHY 是 USB 通信的 “物理层执行者”，负责将数字逻辑信号转换为可在物理线缆上传输的电信号，并处理所有与物理传输相关的细节（电气特性、同步、速率等）。它屏蔽了底层硬件的复杂性，使 USB 控制器只需关注数据的逻辑处理（如协议解析、数据包管理），从而实现可靠的 USB 通信。

# 通信协议

## 1.组成关系

域组成了包，不同的域，组成了不同类型的包（Token、Data、HandShake）。

包组成了事务，多个包一起组成了一个事务（SetUp事务，IN 事务，OUT 事务）

事务组成了传输，多个事务，进而组成了不同类型的传输（控制传输，中断传输，批量传输，同步传输）

## 2.包

### 1.包的组成

包（Packet）是USB系统中信息传输的基本单元，所有数据都是经过打包后在总线上传输的。数据在 USB总线上的传输以包为单位，包只能在帧内传输。高速USB总线的帧周期为125us，全速以及低速 USB 总线的帧周期为 1ms。帧的起始由一个特定的包（SOF 包）表示，帧尾为 EOF。EOF不是一个包，而是一种电平状态，EOF期间不允许有数据传输。 

包是USB总线上数据传输的最小单位，不能被打断或干扰，否则会引发错误。若干个数据包组成一次事务传输，一次事务传输也不能打断，属于一次事务传输的几个包必须连续，不能跨帧完成。一次传输由一次到多次事务传输构成，可以跨帧完成。

USB包由五部分组成，即**同步字段（SYNC**）、**包标识符字段（PID）**、**数据字段**、**循环冗余校验字段（CRC）**和包结尾字段（EOP），包的基本格式如下图：

![img](..\figure\8f9fc4429543b57dbd33b4e75d37be8e.png)

其中数据字段有三种：

- ADDR地址字段： **IN/OUT/SETUP令牌包**
- FrameNum帧序号字段：**SOF令牌包**
- DATA数据字段：**DATA数据包**

### 2.PID 域

#### 1.介绍

**PID（Packet Identifier，包标识符）域**是 USB 数据包结构中的关键部分，用于标识数据包的类型和用途，确保通信双方正确解析数据。不同的 PID 标识了不同类型的USB包。由**四位标识符 + 四位标识符反码**构成。这里只用（PID0~4）,PID4~7是PID0~4的取反，用来校验 （偶检验）PID。

![img](..\figure\0fc72bcfab3e315fdc7eb07f5e3f2681.png)

#### 2.包分类

根据 PID 域的不同，主要将 USB 的包分为了 4 类：

1. **令牌包（Token）**【控制信号】 
    - **OUT**（`0001`）：主机向设备的端点发送数据，并包含了设备地址和标号。
    - **IN**（`1001`）：主机请求从设备的端点接收数据，并包含了设备地址和标号。
    - **SETUP**（`1101`）：主机向设备发送控制命令（用于控制传输的初始阶段），用于主机对设备的初始化。
    - **SOF**（`0101`）：帧开始信号（每 1ms 发送一次，用于同步，包含帧编号），并且包含了相应的帧号。
2. **数据包（Data）**【批量传输】 
    - **DATA0**（`0011`）偶数据包
    - **DATA1**（`1011`）：奇数据包，用于交替传输数据，通过编号区分不同批次的数据，解决数据重传或丢失的问题（USB 协议中采用数据翻转机制）。
    - **DATA2**：高速设备的 PID 的同步包。
    - **MDATA**：高速设备分离包，高带宽的同步事务，仅用于高速模式下的批量传输和等时传输。
3. **握手包（HandShake）**【状态反馈】
    - **ACK**（`0010`）：接收方确认数据已正确接收。
    - **NAK**（`1010`）：接收方暂时无法接收数据（如缓冲区满），请求重传。
    - **STALL**（`1110`）：端点因错误而停止工作或者端点被禁止或不支持控制管道请求，需要主机干预。
    - **NYET**（0110）：无响应
4. **特殊包（Special）**
    - **PRE**（`0110`）：高速模式下的预同步信号，用于兼容全速设备。
    - **EOP**（包结束信号）：严格来说不算 PID，但作为数据包的结束标志，由电平变化定义。
    - **分裂事务**
    - **PING 测试**：批量传输、控制传输的流控探测。

#### 3.数据传输方向

|  PID  | 数据传输方向 |
| :---: | :----------: |
|  IN   | Device->Host |
|  OUT  | Host->Device |
| SETUP | Host->Device |
| PING  | Device->Host |

### 3.地址域

地址域有两部分组成：【 7bits 的设备地址 ADDR + 4 bits 的端点地址 ENDP】![img](..\figure\bc5750be5303fa093711292c2a491d9f.png)可以知道，USB 系统最大支持链接 127 个设备，每个设备最多 2^4=16 个端点。这个 ENDP 只用在 IN/OUT/SETUP令牌包中。

### 4.Frame Number 帧号域

当USB令牌包的 PID 为 SOF时候，其数据字段必须为11位的帧序列号。

帧号占11位，主机每发出一个帧，帧号都会自加1，当帧号达到0x7FF时，将归零重新开始计数。对于同步传输有重要意义。

SOF包的主要作用：

1. **帧同步：** SOF 包作为帧的起始标记，使得所有的 USB 设备能够同步到同一个时间基准上。通过 SOF 包，所有设备可以准确地知道当前处于哪个帧（**由帧号判断在第几帧**），从而实现同步通信。
2. **时钟同步：** SOF 包的周期性发送有助于 USB 设备保持内部时钟的准确性。设备可以通过检测 SOF 包的到来来调整自己的时钟，保证系统时钟的同步。
3. **唤醒设备：** SOF 包可以用来唤醒处于低功耗状态的 USB 设备。当设备检测到 SOF 包时，会从休眠状态中恢复，准备进行数据传输。
4. **周期性传输调度：** SOF 包的周期性发送为 USB 设备提供了固定的时间基准，使得设备可以根据 SOF 包来安排周期性的数据传输。
5. **错误检测：** SOF 包中的 CRC 校验可以帮助检测数据传输过程中的错误。如果设备收到的 SOF 包校验失败，则可以判定数据传输可能存在问题。

### 5.Data 数据域

仅存在于 DATA 信息包，根据不同的传输类型，拥有不同大小的字节（0--1023字节）

### 6.CRC域

用于进行数据的 CRC 校验

## 3.包类型（Packets Type）

### 1.令牌包（Token）

根据域的 PID 描述，令牌包有4种：

令牌包有四种：

- OUT： 通知设备将要输出一个数据包

- IN： 通知设备返回一个数据包
- SETUP： 只用在控制传输中，也是通知设备将要输出一个数据包，与OUT令牌的区别是：只使用DATA0数据包，且只能发到device的控制端点
- SOF： 在每帧开始时以广播的形式发送，针对USB全速设备，主机每1ms/125us产生一个帧，USB主机会对当前帧号进行统计，每次帧开始时通过SOF包发送帧号。

### 2.令牌包格式

- 输入包（IN）、输出包（OUT）和设置包（SETUP）的格式都是一样的：

  ```c
  SYNC + PID + ADDR(7 bits) + ENDP(4bits) + CRC5(五位的校验码)
  ```

- 帧起始包（SOF）的格式

  ```c
  SYNC + PID + 11位FRAM + CRC5(五位的校验码)
  //- SOF包由Host发送给Device
  //- 对于full-speed总线，每隔1.00 ms ±0.0005 ms发送一次；
  //- 对于high-speed总线，每隔125 μs ±0.0625 μs发送一次；
  ```

### 3.数据包（Data）

分为DATA0包和DATA1包，当USB发送数据的时候，如果一次发送的数据长度大于相应端点的容量时，就需要把数据包分为好几个包，分批发送，DATA0包和DATA1包交替发送，即如果第一个数据包是DATA0，那第二个数据包就是DATA1。但也有例外情况，在同步传输中(四类传输类型中之一)。

### 4.数据包格式

所有的数据包都是为DATA0，格式如下

  ```c
  SYNC + PID + 0~1023字节 + CRC16
  ```

### 5.握手包（Handshake）

结构最为简单的包，格式如下

```c
SYNC + PID

//ACK： 传输正确完成
//NAK： 设备暂时没有准备好接收数据，或没有准备好发送数据
//STALL： 设备不能用于传输
//NYET/ERR： 仅用于高速传输，设备没有准备好或出错
```

------

## 4.事务（Transaction）

### 1.概念分类

在USB上数据信息的一次接收或发送的处理过程称为事务处理（Transaction）。一个事务由一系统packet组成`令牌包 + [数据包] + 握手包`，具体由哪些packet组成，它取决于具体的事务。

USB 事务分为3类：

|     事务      |            描述            |
| :-----------: | :------------------------: |
|  SetUp 事务   | 主机用来向设备发送控制命令 |
| Data IN 事务  |   主机用来从设备读取数据   |
| Data OUT 事务 |   主机用来向设备发送数据   |

### 2.SetUp 事务

- 【正常】的设置事务处理

  ![img](..\figure\57e69c35e0c1112dd26d0c1ac911a28a.png)

- 【设备忙时】的设置事务处理

  ![img](..\figure\d2110c357ef999edd3e3e351152985cc.png)

- 【设备出错】的设置事务处理
  ![img](..\figure\5026ced98eebd4365ac856b5524eac7f.png)

### 3.IN 事务

输入事务处理：表示USB主机从总线上的某个USB设备接收一个数据包的过程。

- 【正常】的输入事务处理

  ![img](..\figure\22b7ab96cee4a2e348a78920564d81d6.png)

- 【设备忙】时的输入事务处理  

  ![img](..\figure\ff245ba55a46cad6645caee373dccb88.png)

- 【设备出错】时的输入事务处理
  ![img](..\figure\c92e1b241c5583072725862c097c195f.png)


### 4.OUT 事务

输出事务处理：表示USB主机把一个数据包输出到总线上的某个USB设备的过程。

- 【正常】的输出事务处理
  ![img](..\figure\b9567f1381d55981c8e249c8dc35d9ad.png)

- 【设备忙时】的输出事务处理

  ![img](..\figure\43dda4ae360f4b8b5f5a5a177127b76c.png)

- 【设备出错】的输出事务处理
  ![img](..\figure\a508ef4e448e33329505355c57021351.png)

# USB标准设备请求

#  USB描述符

## 1.描述符介绍

USB协议为USB设备定义了一套描述设备功能和属性的有**固定结构**的描述符，这些描述符是 USB 设备向主机报告自身信息（如设备类型、功能、端点配置等）的数据结构。

标准的描述符有设备描述符、配置描述符、接口描述符、端点描述符和字符串描述符，还有其他标准描述符，如类描述符。

当 USB 设备接入主机时，主机会通过**控制传输**（通常使用端点 0）读取这些描述符，从而了解设备的特性并加载合适的驱动程序。

在USB设备枚举过程中，主机端的协议软件需要解析从USB设备读取的所有描述符信息。在USB主向设备发送读取描述符的请求后，USB设备将所有的描述符以连续的数据流方式传输给USB主机。主机从第一个读到的字符开始，根据双方规定好的数据格式，顺序地解析读到的数据流。

USB 描述符是设备与主机之间的 “语言”，通过结构化的数据让主机了解设备的功能和通信规则。不同层级的描述符从整体到细节（设备→配置→接口→端点）逐步展开，最终实现设备的正确识别和配置。在 USB 设备开发中，正确定义描述符是确保设备被主机正常识别的关键，是主机识别和配置设备的基础。

## 2.描述符分类

- **设备描述符（Device Descriptor）**

  描述设备的整体信息，是主机读取的第一个描述符。

- **配置描述符（Configuration Descriptor）**

  描述设备的一种工作配置（设备可能支持多种配置，如低功耗 / 高性能模式）。

- **接口描述符（Interface Descriptor）**

  描述一个功能接口（设备的功能可划分为多个接口，如一个复合设备可能包含键盘和鼠标接口）。

- **端点描述符（Endpoint Descriptor）**

   描述接口下的端点（数据传输的具体通道）。

- **字符串描述符（String Descriptor）**

  提供人类可读的字符串信息（如厂商名称、产品名称、序列号），可选（但推荐实现）。

- **关联描述符（Association Descriptor）**
  主要用于复合设备（Composite Device），将设备中相关的接口（Interface）“分组”，明确哪些接口属于同一个功能子设备，帮助主机正确识别和管理复合设备的各个组件，使主机能够为每个功能单元加载对应的驱动（而非为整个复合设备加载单一驱动）。
  
- **其他特殊描述符**

  - **HID 描述符**：用于 HID（人机接口设备，如键盘、鼠标），指向 HID 报告描述符。
  - **报告描述符**：HID 设备专用，定义设备与主机之间的数据格式（如按键、坐标信息）。
  - **设备限定描述符**：高速设备用于描述其在全速模式下的特性。
  
## 3.描述符加载流程

> [!CAUTION]
>
> 每个USB设备只有一个设备描述符。
>
> 一个设备中可包含一个或多个配置描述符，即USB设备可以有多种配置。
>
> 设备的每一个配置中又可以包含一个或多个接口描述符，即USB设备可以支持多种功能（接口），接口的特性通过描述符提供。
>
> 一设备至少要包含设备描述符、配置描述符和接口描述符，如果USB设备没有端点描述符，则它仅仅用默认管道与主机进行数据传输。

1. 设备接入主机后，主机通过端点 0 发送`GET_DESCRIPTOR`请求，先读取**设备描述符**。
2. 主机根据设备描述符中的配置数量，读取**配置描述符**。
3. 主机解析配置描述符，读取其包含的**接口描述符**和**端点描述符**。
4. （可选）主机读取**字符串描述符**，显示设备的友好名称。
5. 主机根据描述符信息加载匹配的驱动，完成设备枚举。

## 4.字符串描述符获取步骤

主机请示得到某个字符串描述符时一般分成两步：

### 1.获取设备支持哪些语言

首先主机向设备发出*USB*标准命令*Get_Descriptor*，其中所使用的字符串的索引值为*0*

```c
bmRequestType(1) + bRequest(1) + wvalue(2) + wIndex(2) + wLength(2)
bmRequestType = 0x80 (standard request -- Device Receive -- Device To Host Data)
bRequest = 0x06 (GET_DESCRIPTOR)
wValue = (0x03< 8) | 0x00 // ((GET_DESCRIPTOR_STR < 8) | 0x00 )
    
//根据us2.0协议9.4.3节描述，获取描述符时`bmRequestType=0x80，bRequest=0x06,`这个是协议规定固定的。`wvalue=0x0100`，高字节表示描述符类型，01表示设备，02表示配置；低字节表示索引。
```

返回的结构如下：该字符串描述符包含双字节的语言*ID*的数组，*wLANGID[0]~wLANGID[x]*指明了设备支持的语言。![image-20250807154208918](..\figure\image-20250807154208918.png)
```c
BYTE usbzhLangageId[4] = 
{
 0x06, //字符串长度
 0x03, //字符串类型
 0x09, //0X0409为美式英语的语言ID
 0x04
 0x04, //0x0804表示简体中文
 0x08
}
或者
BYTE usbzhLangageId[4] = 
{
 0x04, //字符串长度
 0x03, //字符串类型
 0x04, //0x0804表示简体中文
 0x08
}
```

### 2.选定支持语言中的一种去获取描述符

 主机根据自己需要的语言，再次向设备发出*USB*标准命令*Get_Descriptor*，指明所要求得到的字符串的索引值和语言。

```c
bmRequestType = 0x80 (standard request -- Device Receive -- Device To Host Data)

bRequest = 0x06 (GET_DESCRIPTOR)

wValue = (0x03< 8) | index // ((GET_DESCRIPTOR_STR < 8) | index)

wIndex = 0x0409 (English)
//iManufacturer ，iProduct ，iSerialNumber 的值，也就是说，你想获取这3个中的哪个字符串描述，就放哪个对应的 index 进去，比如：iManufacturer = 0x03，iProduct = 0x02，iSerialNumber=0x01
```

这次设备所返回的是*Unicode*编号的字符串描述符，其结构如下：

> [!NOTE]
>
> *bString* 域为设备实际返回的以UNICODE编码的字符串流，我们在编写设备端硬件驱动的时候需要将字符串转换为UNICODE编码，也可以通过一些UNICODE转换工具进行转换。

![image-20250807155623595](D:\MyNote\-\嵌入式开发笔记\figure\image-20250807155623595.png)

------

# USB枚举

## 1.枚举概念

USB 枚举（Enumeration）是设备接入主机后，主机识别设备、获取设备信息并完成配置的过程。整个过程中，设备会经历多个状态切换，USB 协议定义了明确的**状态机**来规范这一流程。

## 2.状态分类及介绍

### 1. 连接状态（Attached）

- **触发条件**：设备物理接入 USB 总线（如插入 USB 接口）。
- **状态特征**：
  - 设备通过 USB 线缆获得供电（总线提供 5V 电压），硬件电路初始化（如内部电源稳定、时钟启动）。
  - 此时设备尚未被主机检测到，仅处于 “物理连接” 状态，不响应任何 USB 命令。
- **状态转换**：设备硬件检测到总线供电稳定后，进入 “上电状态”。

### 2. 上电状态（Powered）

- **状态特征**
  - 设备完成上电初始化，内部电路（如 USB 控制器、端点 0 缓冲区）就绪。
  - 此时设备未被主机复位，无法进行数据通信，仅能检测总线状态（如主机发出的复位信号）。
- **关键动作**：主机通过检测 USB 总线的下拉电阻（全速 / 高速设备在 D+/D - 线上的 15kΩ 下拉电阻）感知到设备接入，随后准备发送复位信号。

### 3. 默认状态（Default）

- **触发条件**：主机向设备发送**复位信号**（总线保持低电平至少 10ms）。
- **状态特征**：
  - 设备接收到复位信号后，进入默认状态，初始化所有寄存器和端点（仅端点 0 可用，其他端点未配置）。
  - 设备获得**默认地址（0x00）**，这是枚举过程中主机与设备通信的临时地址（所有新接入设备的初始地址均为 0x00）。
  - 端点 0 被初始化为**控制传输端点**（支持默认的最大包长：低速设备 8 字节，全速设备 64 字节，高速设备 64 字节），用于后续与主机交互。
- **状态转换**：主机通过默认地址（0x00）与设备的端点 0 通信，开始读取设备描述符并配置地址。

### 4. 地址状态（Address）

- **触发条件**：主机向设备发送**设置地址请求（Set Address）**，为设备分配唯一的 USB 地址（1~127 之间）。
- **状态特征**：
  - 设备接收并存储主机分配的地址，后续通信将使用该地址（不再使用默认地址 0x00）。
  - 此时设备仍未被配置（无有效配置描述符），仅能响应与地址相关的命令和基本的控制传输（如读取描述符）。
  - 端点 0 仍为主要通信接口，其他端点仍未激活。
- **状态转换**：主机使用新地址与设备通信，开始读取设备的配置描述符、接口描述符等详细信息。

### 5. 配置状态（Configured）

- **触发条件**：主机向设备发送**设置配置请求（Set Configuration）**，指定设备使用某一配置（配置描述符中定义的配置编号）。
- **状态特征**：
  - 设备根据主机指定的配置，加载对应的配置参数（如激活接口、端点，设置端点的传输类型、最大包长等）。
  - 所有配置中定义的端点（除端点 0 外）被激活，可开始正常的数据传输（如批量传输、中断传输等）。
  - 设备此时处于 “可用状态”，可响应主机的功能请求（如 U 盘的读写命令、鼠标的中断报告等）。
- **状态转换**：枚举过程完成，设备进入正常工作状态。

### 6. **挂起状态（Suspended）**

- **触发条件**：USB 总线在 3ms 内无数据传输（空闲状态），或主机主动发送挂起命令。
- **状态特征**：
  - 设备进入低功耗模式，关闭非必要电路（如高速时钟、外设模块），仅保留唤醒检测电路。
  - 设备停止响应正常数据传输，但能检测总线活动（如主机发送的唤醒信号或数据）。
- **状态转换**：当总线出现数据传输（如主机发送数据包）或设备自身触发唤醒（如鼠标移动），设备退出挂起状态，恢复到配置状态。

## 3.AT32F407A芯片寄存器

### 1.上电状态

主机需要检测 USB 总线的下拉电阻（全速 / 高速设备在 D+/D - 线上的 15kΩ 上拉电阻）感知到设备接入，随后准备发送复位信号。而此芯片上的USB设备为USBFS，因此需要使能上拉电阻，且使能USB PHY(**实现编码，以及差分信号转换的硬件电路**，负责处理数字信号【编码方式】与物理电信号【电平标识】的转换)，让主机能够检测到。

```c
USBFS_CTRL.DISUSB = 1;
USBFS_CFG.PUO = 1;
```

## 2.上电状态——>默认状态

主机向设备发送**复位信号**（总线保持低电平至少 10ms），触发复位中断。中断处理函数里初始化所有寄存器和端点，设置设备地址为端点0，使能USB Core，进入默认状态。

```c
  usbx->devaddr_bit.addr = address;
  usbx->devaddr_bit.cen = TRUE;
```

# 参考链接

[USB 设备描述符 - USB中文网](https://www.usbzh.com/article/detail-104.html)

[【USB笔记】 标准设备请求Standard Device Requests_usb standard req-CSDN博客](https://blog.csdn.net/u012028275/article/details/114712631)

[CDC 特定类请求 - USB中文网](https://www.usbzh.com/article/detail-1502.html)

[USB 协议分析（含基本协议和 USB 请求和设备枚举）_usb通讯时的枚举类型-CSDN博客](https://blog.csdn.net/zhoutaopower/article/details/82083043)

[STM32 USB如何配置多个CDC设备---5个CDC设备 - 知乎](https://zhuanlan.zhihu.com/p/345252630)

[IRIG-B码解析指南-CSDN博客](https://blog.csdn.net/dyx1993/article/details/114655922)

# ——USB Class——

# USB CDC

## 1.概念介绍

**USB CDC** 是 **USB Communication Device Class**（USB 通信设备类）的缩写，是一种协议规范，是 USB 协议中定义的一种设备类规范，用于让 USB 设备模拟传统的通信接口（如串口、调制解调器等），实现数据传输功能。

## 2.核心作用

简单来说，USB CDC 允许 USB 设备被电脑（或其他主机）识别为 “通信设备”，从而通过标准的串口通信协议（如 UART）进行数据交互。例如：

- 单片机通过 USB 接口连接电脑时，若遵循 CDC 规范，电脑会将其识别为一个 “虚拟串口”（如 Windows 中的 `COMx`，Linux 中的 `/dev/ttyACM0`）；
- 开发者可以像操作传统串口一样，用 `Putty`、`Serial Monitor` 等工具与设备通信，无需额外安装复杂驱动。

## 3.主要特点

1. **模拟传统通信接口**
   最常见的应用是 **USB 转串口**（USB-to-Serial），即通过 CDC 规范让 USB 设备模拟 RS-232/RS-485 等串口，兼容传统串口通信软件（如串口调试助手）。
2. **无需定制驱动（主流系统）**
   Windows、Linux、macOS 等主流操作系统已内置 USB CDC 通用驱动，设备只需遵循规范，即可 “即插即用”，无需用户手动安装驱动（特殊子类可能需要额外驱动）。
3. **支持多种通信模式**
   USB CDC 包含多个子类（Subclass），适应不同通信场景：
   - **ACM（Abstract Control Model）**
     最常用的子类，支持控制信号（如 DTR、RTS）和数据传输，广泛用于单片机、模块（如 ESP32、STM32 的 USB 串口功能）。
   - **Ethernet Networking Control Model**
     用于 USB 以太网适配器（如 USB 网卡）。
   - **AT Command Model**
     模拟调制解调器（Modem）的 AT 指令集，早期用于拨号上网设备。

## 4.典型应用场景

- **嵌入式开发**
  单片机（如 STM32、Arduino Leonardo）通过 USB CDC 模拟串口，与电脑传输调试信息、接收控制命令。
- **物联网设备**
  传感器模块通过 USB CDC 将数据发送到电脑，或接收配置参数。
- **消费电子**
  智能手表、打印机等设备通过 CDC 实现与主机的简单数据交互。

## 5.USB虚拟串口和USB转串口

USB 转串口驱动和 USB 虚拟串口（Virtual COM Port，VCP）本质上都是为了在 USB 总线上实现串口通信功能，但它们的实现方式、应用场景和技术原理存在显著区别，核心差异在于**硬件形态**和**功能定位**。

### 1. 定义与硬件基础

- **USB 转串口驱动**
  用于支持**物理 USB 转串口适配器**（硬件设备）的驱动程序。这类适配器是一个实体硬件（如常见的 “USB to TTL 模块”“USB 转 RS232/485 转换器”），其内部包含 USB 控制器芯片（如 CH340、PL2303、FT232 等）和串口电平转换电路，能将 USB 信号物理转换为标准串口信号（RS232/485/TTL 电平），从而连接传统串口设备（如单片机、传感器、老式 Modem）。
  驱动的作用是让操作系统识别这个硬件适配器，并将其映射为系统中的一个 “串口设备”（如 Windows 中的`COMx`），使上层软件（如串口调试助手）能通过标准串口 API 与其通信。
- **USB 虚拟串口（VCP）**
  是纯软件层面的串口模拟，**不需要额外硬件**。它通过驱动程序在 USB 设备（如手机、嵌入式开发板、虚拟机等）内部模拟出一个 “虚拟串口”，使设备能通过 USB 总线与主机进行类似串口的通信，但实际数据传输完全基于 USB 协议，不涉及物理串口电平转换。例如，当手机通过 USB 连接电脑并开启 “USB 调试” 时，可能会在电脑上生成一个虚拟串口，供调试工具（如 ADB）通过串口协议格式进行通信，但实际底层是 USB 数据传输。

### 2. 技术原理差异

- **USB 转串口驱动**
  硬件适配器的 USB 芯片会遵循 USB 协议中的 “串口设备类（USB CDC-ACM，Communication Device Class - Abstract Control Model）” 规范，或厂商自定义的 USB 设备类。驱动程序需要：
  1. 识别硬件的 USB 设备 ID（VID/PID），加载对应的驱动逻辑；
  2. 将主机发送的串口数据（如波特率、数据位、停止位配置）通过 USB 协议传递给硬件芯片；
  3. 硬件芯片将 USB 数据转换为物理串口信号（电信号），或反之将串口信号转换为 USB 数据包；
  4. 向上层软件提供标准串口接口（如`CreateFile`、`ReadFile`、`WriteFile`），屏蔽 USB 底层细节。
- **USB 虚拟串口**
  本质是 USB 设备与主机之间约定的一种 “串口数据格式封装”，完全通过软件实现：
  1. USB 设备（如开发板）在固件中实现虚拟串口逻辑，将需要传输的数据按照串口协议格式（如帧头、校验位）打包成 USB 数据包；
  2. 主机端的虚拟串口驱动识别这些 USB 数据包，解析出串口格式的数据，并映射为系统中的`COMx`端口；
  3. 数据传输全程在 USB 总线上完成，没有物理串口线和电平转换，仅模拟了串口的通信协议和 API 接口。

### 3. 应用场景区别

- **USB 转串口驱动**
  适用于**需要连接物理串口设备**的场景，解决传统串口设备（无 USB 接口）与现代 USB 主机的兼容性问题。例如：
  - 用 USB 转 TTL 模块连接 51 单片机、ESP32 等嵌入式设备进行程序下载或调试；
  - 用 USB 转 RS232 转换器连接老式工业设备（如 PLC、全站仪）的串口接口。
- **USB 虚拟串口**
  适用于**纯 USB 设备需要模拟串口通信**的场景，避免额外硬件成本，简化开发。例如：
  - 智能手机通过 USB 虚拟串口与电脑传输调试信息（如 Android 的 ADB 虚拟串口）；
  - 嵌入式开发板（如 STM32）通过内置 USB 控制器模拟串口，直接与电脑通信（无需外接 USB 转串口芯片）；
  - 虚拟机与宿主机之间通过虚拟串口传输数据。

### 4. 核心区别总结

| 对比维度 | USB 转串口驱动                          | USB 虚拟串口（VCP）                     |
| -------- | --------------------------------------- | --------------------------------------- |
| 硬件依赖 | 必须配合物理 USB 转串口适配器           | 无额外硬件，依赖设备内置 USB 控制器     |
| 信号转换 | 涉及物理串口电平（RS232/TTL 等）转换    | 无物理信号转换，纯软件协议模拟          |
| 本质作用 | 驱动物理硬件，实现 USB 与串口的物理桥接 | 模拟串口协议，实现 USB 上的串口格式通信 |
| 典型应用 | 连接传统串口设备（单片机、工业设备）    | 嵌入式设备调试、手机 USB 调试等         |

### 5.总结

- USB 转串口驱动是 “硬件驱动”，负责让物理转换设备工作，解决 “老设备连新主机” 的问题；
- USB 虚拟串口是 “软件模拟”，在纯 USB 设备上模拟串口功能，解决 “新设备需要串口协议” 的问题。
- 单片机通过USB虚拟串口接收上行数据用到的就是虚拟串口，下行转发数据到485就是用的串口协议，**即转发上行虚拟串口数据到下行485实际串口的单片机可以看成一个更加智能的USB转串口芯片。**
- 两者最终都能向上层软件提供标准串口接口，但底层实现和应用场景完全不同。

## 6.虚拟串口与传统串口的区别

| 对比项   | 传统硬件串口（如 RS-232）  | USB CDC 虚拟串口                                    |
| -------- | -------------------------- | --------------------------------------------------- |
| 物理接口 | 专用串口（DB9 等）         | USB 接口（Type-A/C 等）                             |
| 传输速率 | 较低（通常最高 115200bps） | 更高（取决于 USB 版本，如 USB 2.0 可达 480Mbps）    |
| 驱动需求 | 可能需要专用驱动           | 主流系统内置通用驱动                                |
| 扩展性   | 受限于硬件接口数量         | 可通过 USB 集线器扩展多个虚拟串口（**比如扩展坞**） |

总之，USB CDC 是一种让 USB 设备便捷实现通信功能的标准，尤其在嵌入式领域，它简化了设备与主机的连接流程，是串口通信在 USB 时代的重要替代方案。





# ——USB实现框架——

# 默认管道

## 1.默认管道概念

USB 协议中的 “默认管道（Default Pipe）” 是**设备与主机之间在枚举阶段（设备刚插入时）唯一存在的通信通道**，用于设备初始化和配置，即使设备没有其他端点描述符，也能通过它与主机交互。

## 2.默认管道的特性

1. **基于端点 0（Endpoint 0）**
   默认管道本质上是**端点 0 的逻辑抽象**，而端点 0 是所有 USB 设备必须强制实现的端点（其他
2. **生命周期贯穿设备整个连接过程**
   - 设备插入主机后，首先通过默认管道完成枚举（主机读取设备描述符、配置设备）；
   - 即使枚举完成后，默认管道依然存在，用于处理后续的控制命令（如重新配置设备、获取状态等）。
3. **固定的通信规则**
   - 采用控制传输模式，数据包大小受限于端点 0 的`wMaxPacketSize`（通常为 8/16/32/64 字节）；
   - 传输过程严格遵循 “令牌包→数据包→握手包” 的 USB 控制传输流程，确保可靠性。

## 3.默认管道的作用

1. **设备枚举的唯一通道**
   设备刚插入时，主机对设备一无所知（不知道设备类型、支持的端点等），只能通过默认管道（端点 0）发送`GET_DESCRIPTOR`等请求，逐步获取设备信息（如设备描述符、配置描述符），完成识别过程。
2. **设备配置与管理**
   枚举完成后，主机仍通过默认管道向设备发送控制命令，例如：
   - 切换设备的配置（`SET_CONFIGURATION`）；
   - 获取设备状态（`GET_STATUS`）；
   - 重置端点（`CLEAR_FEATURE`）等。
3. **兼容低功能设备**
   对于仅需简单通信的设备（如某些传感器），可以不定义其他端点，完全通过默认管道（端点 0）的控制传输实现数据交互（尽管效率较低，但能简化硬件设计）。

## 4.与 “非默认管道” 的区别

| 特性       | 默认管道（Endpoint 0）          | 非默认管道（如 Endpoint 1-15）    |
| ---------- | ------------------------------- | --------------------------------- |
| 必要性     | 强制存在（所有 USB 设备必须有） | 可选（按需定义）                  |
| 传输类型   | 仅支持控制传输                  | 可支持批量、中断、同步传输        |
| 用途       | 枚举、配置、管理                | 实际数据传输（如文件读写）        |
| 包大小限制 | 通常较小（如 64 字节）          | 可更大（如 512 字节，取决于设备） |

## 5.总结

“默认管道” 是 USB 设备通过端点 0 与主机建立的基础通信通道，是设备从插入到正常工作的 “必经之路”。即使设备没有其他端点描述符（即不支持批量、中断等传输），也能通过默认管道的控制传输完成基本通信，这也是 USB 协议兼容性的重要保障。

# USB Core

## 1.概念

在 USB 协议和硬件架构中，**USB Core（USB 核心）** 是指负责处理 USB 协议核心逻辑、协调各组件工作的关键模块，通常以**硬件电路（如芯片内的控制器）**或**软件驱动（如操作系统中的 USB 核心驱动）**的形式存在。它是 USB 通信的 “中枢系统”，**承上启下**，确保数据传输符合 USB 协议规范。

## 2.USB Core 的核心功能

1. **协议解析与执行**
   实现 USB 协议的核心逻辑，包括：
   - 解析主机与设备之间的各类数据包（如令牌包、数据包、握手包），理解 PID（包标识符）、地址、端点等关键信息。
   - 执行 USB 传输流程（如控制传输的设置 / 数据 / 状态阶段、批量传输的分包与拼接等），确保数据交互遵循协议规则。
   - 处理设备枚举过程（设备插入后，主机通过 USB Core 完成设备识别、驱动加载、配置信息获取等关键步骤）。
2. **资源管理与调度**
   - 管理 USB 总线上的设备地址（确保每个设备有唯一地址）和端点资源（分配端点编号、监控端点状态）。
   - 调度不同类型的传输（控制传输、批量传输、中断传输、等时传输），根据优先级和实时性要求分配总线带宽（如等时传输优先保障带宽，控制传输优先保障可靠性）。
3. **与硬件 / 软件的交互**
   - **向下对接 USB PHY**：向 PHY 发送数字信号（如待传输的数据），或从 PHY 接收转换后的数字信号，屏蔽物理层的电气特性细节。
   - **向上对接上层软件 / 应用**：在主机端，USB Core 向操作系统提供统一的接口（如 USB 驱动模型），让设备驱动或应用程序能通过标准化 API 访问 USB 设备；在设备端，USB Core 向设备固件提供接口，用于配置端点、处理传输请求等。
4. **错误处理与状态管理**
   - 检测传输错误（如数据包校验失败、超时、端点挂起等），并执行协议规定的恢复机制（如重传、清除挂起状态）。
   - 维护 USB 总线和设备的状态（如总线空闲、传输中、挂起、恢复等），并根据状态变化触发相应操作（如进入低功耗模式）。

## 3.USB Core 的存在形式

- **硬件层面**：在 USB 控制器芯片（如 PC 的 USB 主机控制器、嵌入式设备的 USB 从控制器）中，USB Core 通常是一个硬件模块，以电路逻辑实现协议解析、传输调度等核心功能，提升处理效率，**可以理解为组帧解帧电路逻辑**。
- **软件层面**：在操作系统中（如 Windows 的`usbport.sys`、Linux 的`usbcore`模块），USB Core 表现为核心驱动程序，负责管理 USB 主机控制器、设备枚举、驱动匹配等系统级功能，是连接硬件与上层应用的桥梁，**可以理解为根据硬件层解析结果进行处理的软件逻辑**。

## 4.总结

USB Core 是 USB 系统的 “协议大脑”，其核心作用是**实现 USB 协议的核心逻辑、管理资源、调度传输，并协调上下层交互**。无论是主机端还是设备端，都需要 USB Core 来确保数据传输符合 USB 规范，是 USB 通信不可或缺的核心组件。

简单来说，USB PHY 负责 “信号的物理转换”，而 USB Core 负责 “协议的逻辑执行”，两者配合完成

从数字信号到协议通信的完整流程。

