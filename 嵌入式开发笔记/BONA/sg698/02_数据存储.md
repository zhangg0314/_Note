

# 旧数据库（dblite）

## 1.介绍

`dblite`目录是由`Sqlite3`来实现的数据库接口，采用多态的方式定义了多个数据库接口。比如存`DLMS`参数的数据库，存`DLMS`抄表记录的数据库、存集中器事件的数据库等，即一个`.db`数据库文件存一种大类的数据，然后一个大类又分很多类，即一个`.db`文件里又有很多表table，比如抄读表的数据存在同一个`.db`数据库中，但表的数据有冻结数据，事件等，所有冻结数据和事件又各自开一张表table，这样分的细。旧数据库最早寸的是698相关的，后面海外需求新增了`DLMS`。

## 2.结构介绍

文件位置：`DBLiteInterface.h、DBLiteInterface.cpp`

### 1.枚举定义

- 主要查询类型

  ```c++
  enum ENUM_MAIN_QUERY_TYPE
  {
     QUERY_BY_SAVETIME = 0,     //存储时间
     QUERY_BY_STARTTIME = 1,    //开始时间
     QUERY_BY_SUCTIME = 2,      //采集成功时间
     QUERY_BY_NTH = 3,          //上第n条记录
     QUERY_BY_NTATOL = 4,       //上n条记录
     QUERY_BY_HAPTIME = 5,      //事件发送时间
     QUERY_BY_FINISHTIME = 6,   //事件完成事件
     QUERY_BY_HAPTIME_AND_MPNO = 7,
     QUERY_BY_FINISHTIME_AND_MPNO = 8,
     QUERY_BY_SEQ = 9,           //序列号
     QUERY_BY_FREEZETIMEUNIQUE = 10,//冻结事件
     QUERY_BY_MPNO = 11, //表号
     QUERYLSTEVT_BY_MPNO = 12,
     QUERY_BY_SUCTIME_ALLMP = 13,
     QUERY_BY_UPTIME = 14,
  };
  ```

  

# 新数据库（dlmsdb）

# GDBM存储

# XML存储