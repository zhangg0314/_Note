# 系统环境

```shell

```



```shell
sudo apt-get install debhelper autotools-dev

sudo apt-get install autoconf automake libtool
```

```
编译器太老。GCC 5 的 C++14 支持不完整，std::hash 对 enum class 的自动特化没实现。

  两条路：

  选 1：升级编译器（推荐）
  sudo apt-get install g++-7
  # 然后编译时用
  CXX=g++-7 make

  选 2：不改编译器，代码里补 hash
  在 HttpApiError.hpp 里加 std::hash<BusinessErrno> 特化。

  你那个编译环境 /usr/include/c++/5/ 是 Ubuntu 16.04 的，太老了。装 g++-7 一把解决。

✻ Churned for 8s

❯ dpkg-buildpackage: error: debian/rules build gave error exit status 2
  zg@zg-virtual-machine:~/sg698$ sudo apt-get install g++-7
  Reading package lists... Done
  Building dependency tree
  Reading state information... Done
  E: Unable to locate package g++-7
  E: Couldn't find any package by regex 'g++-7'


  Thought for 2s (ctrl+o to expand)

● Ubuntu 16.04 太老，g++-7 不在默认源。试试：

  # 先看看源里有啥
  apt-cache search g++ | grep -E 'g\+\+-[0-9]'

  # 如果没有，加 PPA
  sudo add-apt-repository ppa:ubuntu-toolchain-r/test
  sudo apt-get update
  sudo apt-get install g++-7

  或者用系统已有的最高版本：

  # 16.04 默认可能能装到 g++-5 以上
  sudo apt-get install g++-6
  
  
  # 切换默认编译器
  sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-7 70
  sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-7 70

  # 选版本
  sudo update-alternatives --config g++
  sudo update-alternatives --config gcc

  或者编译时临时指定，不改系统默认：

  CXX=g++-7 CC=gcc-7 dpkg-buildpackage -us -uc

  推荐 update-alternatives，一次设好省心。
```

